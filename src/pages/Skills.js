import classes from "./Skills.module.css";

function SkillsPage() {
  return (
    <section className={classes.main}>
      <h1 className={classes.pageTitle}>Skills</h1>
    </section>
  );
}

export default SkillsPage;
